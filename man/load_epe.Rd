% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epe.R
\name{load_epe}
\alias{load_epe}
\title{EPE}
\usage{
load_epe(dataset, geo_level = "state", raw_data = FALSE, language = "eng")
}
\arguments{
\item{dataset}{Dataset name: "consumer_energy_consumption", "industrial_energy_consumption",
"national_energy_balance", or "energy_state_panel".
\itemize{
\item "national_energy_balance" covers 1970-2025 (yearly), reading EPE's consolidated BEN
dashboard table -- account labels are the source's own wording (e.g. "Refinarias de
Petroleo"), not a synthetic reconstruction; a "tipo"/"type" column
(Fontes de Energia Primaria/Secundaria/Total) is included.
\item "energy_state_panel" covers 2011-2025 (yearly), one row per state.
\item "consumer_energy_consumption" and "industrial_energy_consumption" cover 2004-2025
(monthly).
}}

\item{geo_level}{Geographical level: "state" or "subsystem". Only applies to
"consumer_energy_consumption" and "industrial_energy_consumption". "region" is not
currently supported for either dataset -- passing \code{geo_level = "region"} will fail
during download, not at validation time.}

\item{raw_data}{A \code{boolean} setting the return of raw (\code{TRUE}) or processed (\code{FALSE}) data.}

\item{language}{A \code{string} that indicates in which language the data will be returned. Portuguese ("pt") and English ("eng") are supported.}
}
\value{
A \code{list} of tibbles (if \code{raw_data} = \code{TRUE}) or a tibble (if \code{raw_data} = \code{FALSE}).
}
\description{
Electrical Energy Monthly Consumption per Class or Industrial Sector
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
### DO NOT RUN ###
# download treated (raw_data = FALSE) data about
# consumer energy consumption (dataset = "consumer_energy_consumption")
# at the state level (geo_level = "state")
data <- load_epe(
  dataset = "consumer_energy_consumption",
  geo_level = "state",
  raw_data = FALSE
)
# download treated (raw_data = FALSE) data
# from the National Energy Balance (dataset = "national_energy_balance")
balance <- load_epe(
  dataset = "national_energy_balance",
  raw_data = FALSE
)
# download treated (raw_data = FALSE) data
# from the State Energy Production Panel (dataset = "energy_state_panel")
panel <- load_epe(
  dataset = "energy_state_panel",
  raw_data = FALSE
)
\dontshow{\}) # examplesIf}
}
